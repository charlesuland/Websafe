# Websafe
This is the project folder for WebSafe, a secure website builder and content management system.
The frontend is composed with Vue.js in the frontend folder.
The backend is composed with FastAPI in the backend folder.
To start the project, move into frontend and use npm run dev. Then move into the backend and start FastAPI
the .venv file in the backend is ignored on git.
